[project]
name = "cortex-graphify-bridge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "graphifyy>=0.4.0",
    "networkx>=3.0",
]

[project.scripts]
cortex-scan = "cortex_bridge.cli:main"
